<Window x:Class="WoWAddonIDE.Windows.GitHubSignInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign in with GitHub" Height="220" Width="520" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="1) Click 'Open GitHub' and sign in (passkeys/2FA supported)."
               Grid.Row="0" Margin="0,0,0,6"/>
        <TextBlock Text="2) Enter the code below on the GitHub page if prompted:"
               Grid.Row="1" Margin="0,0,0,6"/>
        <TextBox x:Name="UserCodeBox" FontFamily="Consolas" FontSize="18" IsReadOnly="True"
             HorizontalContentAlignment="Center" Grid.Row="2" VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Open GitHub" Width="120" Margin="0,0,8,0" Click="OpenGitHub_Click"/>
            <Button Content="Cancel" Width="100" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>