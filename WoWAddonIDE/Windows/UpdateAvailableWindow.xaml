<Window x:Class="WoWAddonIDE.Windows.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available" Height="320" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource App.BackgroundBrush}"
        Foreground="{DynamicResource App.ForegroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"
                   Text="A new version of WoW Addon IDE is available!"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock>
                <Run Text="Current version: "/>
                <Run x:Name="CurrentVersionRun" FontWeight="SemiBold"/>
            </TextBlock>
            <TextBlock>
                <Run Text="Latest version: "/>
                <Run x:Name="LatestVersionRun" FontWeight="SemiBold" Foreground="Green"/>
            </TextBlock>
        </StackPanel>

        <TextBox x:Name="ReleaseNotesBox" Grid.Row="2"
                 IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="11"
                 Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Download" Width="100" Margin="0,0,8,0" Click="Download_Click"/>
            <Button Content="View Release" Width="100" Margin="0,0,8,0" Click="ViewRelease_Click"/>
            <Button Content="Later" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
