<Window x:Class="WoWAddonIDE.Windows.FrameDesignerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Frame XML Designer"
        Width="1100" Height="720"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource App.BackgroundBrush}"
        Foreground="{DynamicResource App.ForegroundBrush}"
        KeyDown="Window_KeyDown">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" Margin="8,8,8,4" Background="{DynamicResource App.BackgroundBrush}">
            <Button Content="Add Frame" Click="AddFrame_Click" Padding="8,4" Margin="0,0,4,0"/>
            <Button Content="Add Child" Click="AddChild_Click" Padding="8,4" Margin="0,0,4,0"/>
            <Button Content="Remove" Click="RemoveFrame_Click" Padding="8,4" Margin="0,0,4,0"/>
            <Separator/>
            <Button Content="Add Anchor" Click="AddAnchor_Click" Padding="8,4" Margin="0,0,4,0"/>
            <Button Content="Add Script" Click="AddScript_Click" Padding="8,4" Margin="0,0,4,0"/>
            <Separator/>
            <Button Content="Load XML..." Click="LoadXml_Click" Padding="8,4" Margin="0,0,4,0"/>
            <Button Content="Save XML..." Click="SaveXml_Click" Padding="8,4" Margin="0,0,4,0"/>
        </ToolBar>

        <!-- Main content -->
        <Grid Grid.Row="1" Margin="8,0,8,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- Frame hierarchy -->
            <Border Grid.Column="0"
                    BorderBrush="{DynamicResource App.BorderBrush}"
                    BorderThickness="1">
                <DockPanel>
                    <TextBlock Text="Frame Hierarchy" DockPanel.Dock="Top"
                               FontWeight="SemiBold" Margin="8,6,8,4"/>
                    <TreeView x:Name="FrameTree"
                              SelectedItemChanged="FrameTree_SelectedItemChanged"
                              Background="{DynamicResource App.BackgroundBrush}"
                              Foreground="{DynamicResource App.ForegroundBrush}"/>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch"
                          Background="{DynamicResource App.BorderBrush}"/>

            <!-- Property editor -->
            <Border Grid.Column="2"
                    BorderBrush="{DynamicResource App.BorderBrush}"
                    BorderThickness="1">
                <DockPanel>
                    <TextBlock Text="Properties" DockPanel.Dock="Top"
                               FontWeight="SemiBold" Margin="8,6,8,4"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="PropertiesPanel" Margin="8"/>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="3" Width="6" HorizontalAlignment="Stretch"
                          Background="{DynamicResource App.BorderBrush}"/>

            <!-- XML Preview -->
            <Border Grid.Column="4"
                    BorderBrush="{DynamicResource App.BorderBrush}"
                    BorderThickness="1">
                <DockPanel>
                    <TextBlock Text="XML Preview" DockPanel.Dock="Top"
                               FontWeight="SemiBold" Margin="8,6,8,4"/>
                    <TextBox x:Name="XmlPreview"
                             FontFamily="Consolas" FontSize="12"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Background="{DynamicResource App.BackgroundBrush}"
                             Foreground="{DynamicResource App.ForegroundBrush}"
                             BorderThickness="0"/>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Footer -->
        <DockPanel Grid.Row="2" LastChildFill="False" Margin="8,0,8,8">
            <TextBlock x:Name="StatusText" DockPanel.Dock="Left" VerticalAlignment="Center"
                       Margin="4,0,0,0"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Copy XML" Width="90" Click="CopyXml_Click" Margin="0,0,8,0"/>
                <Button Content="Insert into Editor" Width="120" Click="InsertIntoEditor_Click" Margin="0,0,8,0"/>
                <Button Content="Close" Width="80" Click="Close_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
